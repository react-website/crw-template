import React, { memo } from 'react'

import './scss/index.scss'

function Table() {
    return (
        <div styleName="table-page">
            table
        </div>
    )
}

export default memo(Table)
